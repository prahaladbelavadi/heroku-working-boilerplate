# heroku-working-boilerplate

There exists a procfile.

There needs to exist a heroku branch that it gets deployed to.

Code can be committed on master and pushed to heroku branch for the updates to happen.

